# تحديث v3.3.10 - إصلاح Not Found في النماذج

هذا التحديث يعالج ظهور الرسالة:

`{"detail":"Not Found"}`

داخل معاينة مستند قبض وطلب صرف ومستند دفع وكشف صيانة السيارات وتحويل.

## السبب

المحرر يحمّل القوالب من `app/static/form-templates/`، لكن ملفات HTML لم تكن موجودة في نسخة GitHub المنشورة.

## ما تم إصلاحه

- تضمين ملفات HTML الخمسة كاملة داخل التحديث.
- عدم تعديل محتوى القوالب الأصلية.
- إضافة فحص تلقائي يمنع الحفظ إذا تعذر تحميل القالب ويعرض رسالة واضحة بدلاً من صفحة JSON.
- إضافة cache-busting للإطارات حتى لا يعيد المتصفح استخدام استجابة 404 قديمة.
- إضافة اختبار تلقائي لكل رابط قالب للتأكد أنه يعيد HTTP 200.

## بعد نسخ التحديث

```powershell
git status
git add .
git commit -m "Fix missing HTML templates v3.3.10"
git push origin main
```

بعد أن يصبح Render في حالة Live، نفذ `Ctrl + F5` في المتصفح.

لا يحتاج هذا الإصلاح إلى أي SQL أو تغيير في Supabase.
