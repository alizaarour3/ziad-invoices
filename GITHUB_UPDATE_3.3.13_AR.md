# تحديث GitHub إلى v3.3.13 — القروض المباشرة والتقرير

هذا التحديث يبقي صفحة **قروض** كصفحة بيانات عادية وليست فاتورة.

الواجهة تعرض حقول الإضافة مباشرة: الاسم الثلاثي، المبلغ، عدد الأشهر، والمبلغ المحدد.

بعد إضافة القرض يمكنك: مشاهدة، تقرير، تسديد، تعديل، وحذف نهائي حسب الصلاحية. التقرير قابل للطباعة والحفظ PDF ويحتوي على الرصيد وسجل التسديدات.

لا يحتاج التحديث إلى SQL جديد في Supabase.

```powershell
git status
git add .
git commit -m "Improve loans page and add printable reports v3.3.13"
git push origin main
```
