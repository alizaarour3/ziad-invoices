# Patch v3.3.14 — تحسين التحويل والداشبورد

هذا الـPatch مخصص للترقية من **v3.3.13 إلى v3.3.14**.

## ما الذي يتغير؟

- تحسين واجهة صفحة **تحويل** بالكامل من داخل النظام دون تعديل قالب التحويل A4 الأصلي.
- بطاقات إحصائية للتحويلات، إجمالي المبالغ الرقمية، بحث وفلترة حسب الحالة والقسم، وجدول معلومات أوضح.
- رأس مخصص عند مشاهدة أو تعديل طلب التحويل مع عرض التاريخ/الجهة/المبلغ عندما تكون محفوظة.
- إعادة تصميم **الداشبورد** ليعرض مؤشرات حقيقية، حالة الأقسام، التحويلات، آخر المستندات، نشاط 7 أيام، المسودات التي تحتاج متابعة، وملخص القروض عند توفر الصلاحية.
- لا يوجد أي SQL جديد ولا Migration جديد.

بعد نسخ محتويات الـPatch فوق مشروع v3.3.13:

```powershell
git status
git add .
git commit -m "Improve transfer workspace and dashboard v3.3.14"
git push origin main
```

بعد أن يصبح Render `Live` نفّذ `Ctrl + F5`.
