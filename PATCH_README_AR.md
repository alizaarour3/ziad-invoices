# Patch v3.3.15 -> v3.3.16

المحتويات:
- تفعيل RLS تلقائياً على جداول التطبيق عند العمل على Supabase.
- سحب صلاحيات Data API المباشرة من `anon` و`authenticated`.
- حماية الجداول والتسلسلات المستقبلية افتراضياً.
- ملف SQL مستقل لتأمين قاعدة البيانات الحالية والتحقق من النتيجة.

لا يحذف هذا التحديث بيانات ولا يغير قوالب المستندات الرسمية.
