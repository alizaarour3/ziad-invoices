# Patch 3.3.8 — إصلاح عرض النماذج HTML

هذا التحديث مخصص للترقية من **3.3.7 إلى 3.3.8**.

الإصلاحات:
- طلب الصرف يظهر كاملاً بالحجم الصحيح داخل ورقة A4 بدل التكبير الداخلي الخاطئ.
- مستند الدفع وكشف صيانة السيارات لم يعودا يظهران كصفحة بيضاء بسبب اتجاه RTL داخل iframe.
- إصلاح خطأ التنقل بين أسطر الحقول المتعددة في طلب الصرف.
- التكبير والتصغير يبقيان متاحين أثناء الكتابة، والطباعة تبقى A4 100%.
- لم يتم تعديل أي ملف من ملفات القوالب الرسمية.

بعد نسخ ملفات الـPatch فوق المشروع:
```powershell
git status
git add .
git commit -m "Fix exact HTML template preview scaling v3.3.8"
git push origin main
```

بعد أن يصبح Render Live نفّذ Ctrl+F5 في المتصفح.
