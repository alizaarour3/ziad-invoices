# Patch 3.3.9 — تحويل وصلاحيات

هذا التحديث مخصص للترقية من **3.3.8 إلى 3.3.9**.

## ما يضيفه

- صفحة جديدة باسم **تحويل** تستخدم القالب HTML المرفوع.
- ترقيم مستقل للتحويل يبدأ من `TR-000001`.
- صفحة **صلاحيات** للمدير لتحديد الصفحات التي يراها كل مستخدم.
- حماية الصلاحيات في الواجهة وفي API.
- Migration تلقائي لقاعدة البيانات إلى Schema 4.

لا تحتاج إلى تشغيل SQL يدوي في Supabase.

بعد نسخ ملفات الـPatch فوق المشروع:

```powershell
git status
git add .
git commit -m "Add transfer template and page permissions v3.3.9"
git push origin main
```

بعد أن يصبح Render `Live` نفّذ `Ctrl + F5`.

بعد الدخول كمدير افتح **صلاحيات** وحدد الصفحات لكل مستخدم.
