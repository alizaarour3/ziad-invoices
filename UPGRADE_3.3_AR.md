# التحديث إلى الإصدار 3.3.3

## نسخة Windows المحلية

1. أغلق النسخة القديمة.
2. احتفظ بنسخة كاملة من المجلد القديم.
3. فك الإصدار 3.3.3 في مجلد جديد.
4. انسخ مجلد `data` من النسخة القديمة إلى الجديدة فقط.
5. شغّل `setup-windows.bat` لتحديث المتطلبات.
6. شغّل `start-desktop.bat`.
7. تحقق من عدد المستندات والمرفقات ثم جرّب طباعة مستند واحد.

لا تنسخ مجلدات `app` أو `config` أو `templates` من النسخة القديمة، لأنها ستلغي تحسينات الواجهة وخريطة الأسطر والقوالب الرسمية.

## Render وSupabase

1. أنشئ نسخة احتياطية من النظام الحالي.
2. ارفع الكود الجديد إلى نفس المستودع أو فرع إصدار.
3. أبقِ متغيرات البيئة كما هي.
4. انشر الإصدار الجديد.
5. تحقق من `/api/health` وسجلات Render.
6. تأكد من أعداد المستندات والمرفقات في الداشبورد.

قاعدة البيانات والمرفقات في Supabase لا تُستبدل عند نشر الكود الجديد.
